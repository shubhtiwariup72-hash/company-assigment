# Production — replace this with your deployed backend URL (e.g. Render)
# This is overridden by whatever you set in Netlify → Site Settings → Environment Variables
VITE_API_URL=https://your-backend.onrender.com/api
